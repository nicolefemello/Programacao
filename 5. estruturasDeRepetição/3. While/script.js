function criarVariavel(qntdVar, tipoDado) {
    let parametros = [];
    for (let i = 0; i < qntdVar; i++) {
        if (tipoDado.toLowerCase() == 'number') {
            parametros = {}
        } else (tipoDado.toLowerCase() == 'string')
    }
}